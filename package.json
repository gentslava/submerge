{
  "name": "submerge",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "tsc -b && pnpm -F @submerge/web typecheck",
    "lint": "biome ci .",
    "format": "biome format --write .",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.1",
    "typescript": "^6.0.3"
  }
}
