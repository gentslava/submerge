# node_modules
node_modules/
.pnpm-store/
npm-debug.log*

# TypeScript build output
dist/
*.tsbuildinfo

# v2 server runtime — SQLite DB and secrets, do not commit
packages/server/data/
# Local web/e2e runs may start the server from the web package; never retain
# their SQLite state or Playwright output in the worktree.
packages/web/data/
packages/web/test-results/
.env
.env.*
!.env.example

# mihomo runtime — здесь твои узлы/подписки, НЕ коммитить
mihomo/config.yaml
mihomo/sources.json
mihomo/hwid.txt
mihomo/providers/
mihomo/ui/
mihomo/.cache/
mihomo/cache.db

# Playwright / browser-smoke artifacts
.playwright-mcp/
smoke-*.png

# прочее
*.deb
out/
.DS_Store

# local manual-run stack (mihomo config, sqlite db, dev logs)
.local-run/
# browser-driver scratch (screenshots, snapshots)
.playwright-mcp/
